# Shared config for IR blaster builds
CONFIG_PWM=y
CONFIG_PRINTK=y

# Every IR blaster build is a climate node with battery reporting - see
# node/lib/thread/thread.c (SRP TXT record) and the HA integration's
# capability-based entity creation.
CONFIG_THING_CAPS="ac_climate,battery"
